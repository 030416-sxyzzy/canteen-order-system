<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.canteen.mapper.OrderMapper">
    <resultMap id="orderMap" type="com.canteen.entity.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, amount, status, create_time, update_time)
        VALUES (#{orderNo}, #{userId}, #{amount}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE `order`
        SET order_no = #{orderNo},
            user_id = #{userId},
            amount = #{amount},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="orderMap">
        SELECT * FROM `order` WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="orderMap">
        SELECT * FROM `order` WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="orderMap">
        SELECT * FROM `order` ORDER BY create_time DESC
    </select>

    <select id="selectByOrderNo" resultMap="orderMap">
        SELECT * FROM `order` 
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="getTodayOrderCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM `order` 
        WHERE DATE(create_time) = CURDATE()
    </select>
</mapper> 